<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="value"
            type="com.example.hammered.wrappers.StepsWrapper" />

        <variable
            name="clickListener"
            type="com.example.hammered.cocktail.createCocktail.ClickListener" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/stepNumber"
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:background="@drawable/serial_num_back"
            android:paddingTop="4dp"
            android:paddingStart="2dp"
            android:paddingEnd="2dp"
            android:textAlignment="center"
            android:layout_gravity="center"
            android:textColor="@color/color_black"
            android:textSize="12sp"
            tools:text="1" />

        <LinearLayout
            android:id="@+id/stepsTextContainer"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1">

            <EditText
                android:id="@+id/stepText"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:hint="@string/add_step_hint"
                android:importantForAutofill="no"
                android:inputType="text"
                android:text="@={value.steps}" />

        </LinearLayout>


        <ImageView
            android:id="@+id/removeStep"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:contentDescription="@string/remove_step_desc"
            android:onClick="@{()->clickListener.onClick(value)}"
            android:src="@drawable/cross" />
    </LinearLayout>
</layout>